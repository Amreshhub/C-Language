#include<stdio.h>
#include<conio.h>
void main()
{
int num,sum=0,d,cnum=0;
clrscr();
printf("\nEnter the number:");
scanf("\n%d",&num);
cnum=num;
while(num>0)
{
	d=num%10;
	sum=sum+d*d*d;
	num=num/10;
	}
if(sum==cnum)
	printf("\n%d is amstrong number",cnum);
else
	printf("\n%d is not amstrong number",cnum);
getch();
}
