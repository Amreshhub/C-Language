#include<stdio.h>
#include<conio.h>
#include<dos.h>
void main()
{
char str[100],str1[100];
int a,b;
a1:clrscr();
printf("\nEnter a String");
fflush(stdin);
gets(str);
printf("\nEnter saconed string:");
fflush(stdin);
gets(str1);
if(str[a]!='\0')
{
	printf("\nMust Enter a String:");
	delay(2000);
	goto a1;
	}
for(a=0;str[a]!='\0';a++)
for(b=0;str[b]!='\0';str[a]=str1[b],a++,b++);
printf("\nCoppyed String=%s",str);
getch();
}
