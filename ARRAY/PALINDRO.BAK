#include<stdio.h>
#include<conio.h>
#include<dos.h>
#define size 200
void main(){
int dnum[size],i,f,b,num,cnum;
s1:clrscr();
printf("\nEnter a number:");
scanf("\n%d",&num);
if(num<=0)
{
printf("\nMust enter Positive integer value");
delay(2000);
goto s1;
}
for(i=0;cnum>0;dnum[i++]=cnum%10,cnum=cnum/10);
for(f=0,b=i-1;dnum[f]==dnum[b]&&f<b;f++,b++);
if(f<3)
printf("\n%d is not plindrom",num);
else
printf("\n%d is palindrom no",num);
getch();
}